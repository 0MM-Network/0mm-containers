You are a production Bash expert. On Debian testing (apt install mergerfs if missing), implement as standalone scripts (in folder opencode-ai/artifacts/), rootless MergerFS: union sources from skills.txt (/long/path1\n/long/path2...) into ./skills (live reads/writes, mfs policy). Scripts in current dir: skills-mount.sh (idempotent mount, validate paths/FUSE/mergerfs/perms/exist, --dry-run --verbose --policy=mfs --help, flock lock, traps EXIT/INT/TERM w/ lazy fusermount -u -z), skills-unmount.sh (fuser-km busy check). Strict: set -Eeuo pipefail shopt -s inherit_errexit, getopts, POSIX. Bats tests (mount/unmount/Podman). Output scripts + tests + chmod +x + git diff + bats run.
